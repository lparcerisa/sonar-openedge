function f1 returns logical (table-handle ph-tt):
 return super(table-handle ph-tt).
 // Works with "standard" parameters. optionalFunctionArgs rule only accepts expression, while it "should" accept parameterArg (or parameter).
end function.
